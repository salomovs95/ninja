spring:
  application:
    name: "@project.artifactId@"
  profiles:
    active: ${PROFILE:dev}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:h2:mem:council
    username: 
    password:
